{
  "name": "snake-game",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf www && mkdir www && cp index.html www/index.html",
    "android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/cli": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  }
}